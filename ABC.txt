import pandas as pd

# Assuming you have the data in the respective dataframes:
# tb1_limits_df = pd.read_csv('tb1_limits.csv')  # Replace with your actual data source
# tbl_limits_d1_df = pd.read_csv('tbl_limits_d1.csv')  # Replace with your actual data source

# Performing a left join
merged_df = pd.merge(tb1_limits_df, tbl_limits_d1_df, left_on='KEY', right_on='KEY', how='left', suffixes=('', '_d1'))

# Applying the filter condition
filtered_df = merged_df[(merged_df['RECORD_STAT'] == "0") & (merged_df['KEY_d1'].isna())]

# Selecting the desired columns
result_df = filtered_df[['LIAB_ID', 'LINE_CD', 'LINE_SERIAL', 'MAIN_LINE', 'AVAILABILITY_FLAG', 
                         'LIMIT_AMOUNT', 'DAYLIGHT_OD_LIMIT', 'INTERNAL_REMARKS', 'MAKER_ID', 
                         'RECORD_STAT', 'LINE_START_DATE', 'KEY']]

# Displaying the result
print(result_df)
